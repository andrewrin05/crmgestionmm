# Variables de Entorno para Azure
# Copia este archivo a .env y completa con tus valores reales

# URL de conexión a PostgreSQL (requerido para producción)
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_db
DATABASE_URL=""

# Cadena de conexión a Azure Storage (opcional)
AZURE_STORAGE_CONNECTION_STRING=""

# Nombre del contenedor de Azure Storage (opcional)
AZURE_STORAGE_CONTAINER_NAME="documentos"

# Clave secreta para Flask (opcional, se genera automáticamente si no se proporciona)
FLASK_SECRET_KEY=""

# Ruta del disco de Render para uploads (opcional, usado en producción)
RENDER_DISK_PATH=""
